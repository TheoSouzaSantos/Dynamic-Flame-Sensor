import { DfsmoduleViewProps } from './Dfsmodule.types';

// DfsmoduleView is not available on the web platform.
export default function DfsmoduleView(_props: DfsmoduleViewProps) {
  throw new Error('DfsmoduleView is not available on the web platform.');
}
